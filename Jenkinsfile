pipeline {
    agent any
    stages {
        stage('run') {
            steps {
                echo 'Emrah to Reinvent Yourself'
                sh 'python --version'
                sh 'python pipeline.py'
            }
        }
    }
}
